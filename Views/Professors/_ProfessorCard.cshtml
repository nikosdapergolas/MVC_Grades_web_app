@model VathmologioMVC.Models.Professor

<div class="card" style="width: 18rem; margin: 0.7rem;">
    <img src="https://i.pravatar.cc/286?u=@Model.Afm" class="card-img-top" alt="Card of professor @(Model.Name + " " + Model.Surname)">
    <div class="card-body">
        <h5 class="card-title">@(Model.Name + " " + Model.Surname)</h5>
        <p class="card-text">@Model.Department</p>
        <p class="card-text">Afm: @Model.Afm</p>
        <a asp-action="Details" asp-route-id="@Model.Afm" class="btn btn-info">Show Details</a>
        <a asp-action="Edit" asp-route-id="@Model.Afm" class="btn btn-warning">Edit</a>
        <a asp-action="Delete" asp-route-id="@Model.Afm" class="btn btn-danger">Delete</a>
    </div>
</div>